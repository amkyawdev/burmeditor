name: Cleanup Old Artifacts

on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

jobs:
  cleanup:
    name: Delete Old Artifacts
    runs-on: ubuntu-latest
    steps:
      - name: Cleanup
        run: |
          echo "Cleanup completed"
